import React from 'react';

const Tooltip = (props) => {
    const divStyle = {
      opacity: props.opacity,
      left: props.pageX,
      top: props.pageY
    }

    return (
      <div></div>
      
    );
  }
  }

export default Tooltip;
